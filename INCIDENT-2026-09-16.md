# Unintended re-entry after partial booking

Channel message 13408 announced a new BUY. Message 13410 said “Partial book”
and closed that position. Message 13412 was a profitability update showing
the same source ticket, 2025944568, with a changed SL. The old signal hash
included SL, so the update produced a different hash and opened another BUY
at 4356.74 around 16:38 IST.

The user requested closure of this unintended position. Trade 3 is confirmed
CLOSED with reason USER_REQUEST_UNINTENDED_REENTRY; a subsequent CoinDCX
read confirmed zero nonzero XAU positions.

Entries now require a NEW announcement in the caption or within 120 seconds
before or after the image. Pending associations persist across restart and
are invalidated by exit/update messages. A previously traded source ticket
cannot re-enter, including when SL changes. Legacy stored OCR is checked for
ticket IDs. Result/update wording is rejected in captions and extracted text.

Validation: 46 local checks passed, including 17 new-entry checks also run on
the VPS. The incident message sequence was replayed with mocked trading;
no test orders were placed. Production source and image were updated and
the trader container recreated. Live entries remain configured at $5 margin
and market execution.

The existing daily loss allowance conservatively charges full margin for
closed trades; it is not realized PnL. Its $10 allowance was exhausted by the
two closed trades on September 16. This limit was preserved and resets at
UTC midnight (05:30 IST).

Subsequent user-requested change: both the NEW matching window and maximum
image age are now 1200 seconds (20 minutes). The VPS configuration was
updated, the service restarted, and health confirmed OK. All 49 offline
checks passed before committing, including matching at 19 minutes in either
direction and rejection beyond 20 minutes.
